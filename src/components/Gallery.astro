---
const images = [
  { src: '/images/sala-1.jpg', alt: 'Sala de tratamiento con camilla', shape: 'blob-1' },
  { src: '/images/sala-2.jpg', alt: 'Vista general de la consulta', shape: 'blob-2' },
  { src: '/images/sala-3.jpg', alt: 'Espacio de espera', shape: 'blob-3' },
  { src: '/images/sala-4.png', alt: 'Ambiente relajante de la sala', shape: 'blob-4' },
];
---

<section class="gallery" id="espacio">
  <div class="container">
    <div class="section-header">
      <span class="section-tag">Mi Espacio</span>
      <h2>Un lugar pensado para tu bienestar</h2>
      <p>He diseñado cada rincón para que desde el primer momento sientas calma, confianza y desconexión del estrés.</p>
    </div>
    
    <div class="gallery-grid">
      {images.map((image, index) => (
        <div class={`gallery-item ${image.shape}`} style={`--delay: ${index * 0.15}s`}>
          <div class="gallery-frame">
            <img src={image.src} alt={image.alt} loading="lazy" />
          </div>
          <div class="gallery-decoration"></div>
        </div>
      ))}
    </div>
    
    <div class="gallery-features">
      <div class="gallery-feature">
        <svg viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/></svg>
        <span>Ambiente tranquilo y privado</span>
      </div>
      <div class="gallery-feature">
        <svg viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/></svg>
        <span>Materiales de primera calidad</span>
      </div>
      <div class="gallery-feature">
        <svg viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/></svg>
        <span>Climatización óptima</span>
      </div>
      <div class="gallery-feature">
        <svg viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/></svg>
        <span>Música relajante</span>
      </div>
    </div>
  </div>
</section>

<style>
  .gallery {
    background: var(--color-cream);
    overflow: hidden;
  }
  
  .gallery-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 30px;
    align-items: center;
  }
  
  .gallery-item {
    position: relative;
    animation: fadeInUp 0.6s ease-out forwards;
    animation-delay: var(--delay);
    opacity: 0;
  }
  
  .gallery-frame {
    position: relative;
    overflow: hidden;
    transition: transform 0.5s ease;
  }
  
  .gallery-item:hover .gallery-frame {
    transform: scale(1.02);
  }
  
  .gallery-frame img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.6s ease;
  }
  
  .gallery-item:hover .gallery-frame img {
    transform: scale(1.08);
  }
  
  .gallery-decoration {
    position: absolute;
    z-index: -1;
    transition: transform 0.5s ease;
  }
  
  .gallery-item:hover .gallery-decoration {
    transform: translate(5px, 5px);
  }
  
  /* Organic shapes for each image */
  .blob-1 .gallery-frame {
    border-radius: 60% 40% 50% 50% / 50% 50% 50% 50%;
    aspect-ratio: 1;
  }
  
  .blob-1 .gallery-decoration {
    width: 100%;
    height: 100%;
    border-radius: 60% 40% 50% 50% / 50% 50% 50% 50%;
    background: var(--color-secondary-light);
    top: 10px;
    left: 10px;
  }
  
  .blob-2 .gallery-frame {
    border-radius: 30% 70% 60% 40% / 60% 30% 70% 40%;
    aspect-ratio: 4/5;
  }
  
  .blob-2 .gallery-decoration {
    width: 100%;
    height: 100%;
    border-radius: 30% 70% 60% 40% / 60% 30% 70% 40%;
    background: var(--color-primary-light);
    top: 12px;
    left: -8px;
    opacity: 0.4;
  }
  
  .blob-3 .gallery-frame {
    border-radius: 50% 50% 40% 60% / 40% 60% 40% 60%;
    aspect-ratio: 1;
  }
  
  .blob-3 .gallery-decoration {
    width: 100%;
    height: 100%;
    border-radius: 50% 50% 40% 60% / 40% 60% 40% 60%;
    background: var(--color-secondary);
    top: 8px;
    left: 8px;
    opacity: 0.2;
  }
  
  .blob-4 .gallery-frame {
    border-radius: 40% 60% 55% 45% / 55% 45% 55% 45%;
    aspect-ratio: 4/5;
  }
  
  .blob-4 .gallery-decoration {
    width: 100%;
    height: 100%;
    border-radius: 40% 60% 55% 45% / 55% 45% 55% 45%;
    background: var(--color-primary);
    top: -8px;
    left: 10px;
    opacity: 0.15;
  }
  
  .gallery-features {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 30px;
    margin-top: 60px;
    padding-top: 40px;
    border-top: 1px solid rgba(90, 124, 89, 0.1);
  }
  
  .gallery-feature {
    display: flex;
    align-items: center;
    gap: 10px;
  }
  
  .gallery-feature svg {
    width: 22px;
    height: 22px;
    fill: var(--color-primary);
  }
  
  .gallery-feature span {
    font-size: 0.95rem;
    color: var(--color-text);
    font-weight: 500;
  }
  
  @media (max-width: 1024px) {
    .gallery-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 40px;
    }
  }
  
  @media (max-width: 600px) {
    .gallery-grid {
      grid-template-columns: 1fr;
      max-width: 300px;
      margin: 0 auto;
      gap: 50px;
    }
    
    .gallery-features {
      flex-direction: column;
      align-items: center;
      gap: 15px;
    }
  }
</style>
