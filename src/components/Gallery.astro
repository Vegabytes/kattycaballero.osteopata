---
const images = [
  { src: '/images/sala-1.jpg', alt: 'Sala de tratamiento con camilla' },
  { src: '/images/sala-2.jpg', alt: 'Vista general de la consulta' },
  { src: '/images/sala-3.jpg', alt: 'Espacio de espera' },
  { src: '/images/sala-4.png', alt: 'Ambiente relajante de la sala' },
];
---

<section class="gallery" id="espacio">
  <div class="container">
    <div class="section-header">
      <span class="section-tag">Mi Espacio</span>
      <h2>Un lugar pensado para tu bienestar</h2>
      <p>He diseñado cada rincón para que desde el primer momento sientas calma, confianza y desconexión del estrés.</p>
    </div>
    
    <div class="gallery-grid">
      {images.map((image, index) => (
        <div class="gallery-item" style={`--delay: ${index * 0.15}s`}>
          <img src={image.src} alt={image.alt} loading="lazy" />
        </div>
      ))}
    </div>
    
    <div class="gallery-features">
      <div class="gallery-feature">
        <svg viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/></svg>
        <span>Ambiente tranquilo y privado</span>
      </div>
      <div class="gallery-feature">
        <svg viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/></svg>
        <span>Materiales de primera calidad</span>
      </div>
      <div class="gallery-feature">
        <svg viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/></svg>
        <span>Climatización óptima</span>
      </div>
      <div class="gallery-feature">
        <svg viewBox="0 0 24 24" fill="currentColor"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/></svg>
        <span>Música relajante</span>
      </div>
    </div>
  </div>
</section>

<style>
  .gallery {
    background: var(--color-cream);
  }
  
  .gallery-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 25px;
  }
  
  .gallery-item {
    position: relative;
    border-radius: 20px;
    overflow: hidden;
    aspect-ratio: 3/4;
    box-shadow: 0 10px 40px rgba(0,0,0,0.1);
    animation: fadeInUp 0.6s ease-out forwards;
    animation-delay: var(--delay);
    opacity: 0;
  }
  
  .gallery-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
  }
  
  .gallery-item:hover img {
    transform: scale(1.05);
  }
  
  .gallery-features {
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
    gap: 30px;
    margin-top: 50px;
    padding-top: 40px;
    border-top: 1px solid rgba(90, 124, 89, 0.1);
  }
  
  .gallery-feature {
    display: flex;
    align-items: center;
    gap: 10px;
  }
  
  .gallery-feature svg {
    width: 22px;
    height: 22px;
    fill: var(--color-primary);
  }
  
  .gallery-feature span {
    font-size: 0.95rem;
    color: var(--color-text);
    font-weight: 500;
  }
  
  @media (max-width: 1024px) {
    .gallery-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  
  @media (max-width: 600px) {
    .gallery-grid {
      grid-template-columns: 1fr;
    }
    
    .gallery-item {
      aspect-ratio: 4/3;
    }
    
    .gallery-features {
      flex-direction: column;
      align-items: center;
      gap: 15px;
    }
  }
</style>
